# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.astro/
.netlify/

# Downloaded soundfont assets (run `npm run fetch-soundfonts` to regenerate)
public/audio/soundfonts/

# MCP server config (contiene credenciales locales)
.mcp.json

# Written by `netlify dev` when it bundles the edge functions locally (Deno) — not used by the Netlify build
deno.lock
graphify-out/
# Supabase CLI version cache — written by any `npx supabase` invocation
supabase/.temp/
drive-diag.mjs
drive-diag2.mjs
drive-tmp2.mjs
drive-tmp3.mjs

# Real-song corpus for the audio regression test (user data, never committed)
tests/corpus/data/
