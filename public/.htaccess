Options -MultiViews
RewriteEngine On
RewriteBase /

# Rewrite /editor/<songId> → serve /editor/index.html (SPA routing)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^editor/(.+)$ /editor/index.html [L]
