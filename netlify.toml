[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# SPA fallback for editor deep links
[[redirects]]
  from = "/editor/*"
  to = "/editor/"
  status = 200
