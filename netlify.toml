[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback for editor deep links
[[redirects]]
  from = "/editor/*"
  to = "/editor/"
  status = 200
