// The song page's Practice panel (Sections / Mixer / Tempo / Export expanding under the header,
// opened from a "Practice" button next to Play) was replaced in Sep 2026 by the bottom playback
// bar, which opens the same SongPracticePanel in a sheet. The old inline version is kept, not
// deleted: set this to true and the page shows the Practice button and the inline panel again
// (SongHeaderTransport + #song-practice-panel), exactly as before.
export const LEGACY_PRACTICE_PANEL = false;
