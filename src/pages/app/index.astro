---
export const prerender = true;
import BaseLayout from '../../layouts/BaseLayout.astro';
import SongsApp from '../../components/SongsApp';
---

<BaseLayout
  title="Chord Player — Song Library | Chord Sequence"
  description="Your saved chord progressions on Chord Sequence. Open a song, create a new one, or duplicate and export to WAV."
  canonicalUrl="https://chordsequence.com/app/"
  noIndex={true}
  showNav={false}
>
  <SongsApp client:only="react" />
</BaseLayout>
